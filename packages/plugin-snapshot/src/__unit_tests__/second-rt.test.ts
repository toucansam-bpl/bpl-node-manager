/* tslint:disable:no-floating-promises */
import { describe } from 'riteway'

describe('snapshot', async assert => {
  assert({
    given: '2nd test',
    should: 'be successful',
    actual: 1,
    expected: 1,
  })
})
